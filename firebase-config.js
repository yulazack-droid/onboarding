// Replace the placeholder values below with the configuration copied from:
// Firebase Console -> Project settings -> General -> Your apps -> SDK setup and configuration
export const firebaseConfig = {
  apiKey: "PASTE_API_KEY_HERE",
  authDomain: "PASTE_PROJECT_ID.firebaseapp.com",
  projectId: "PASTE_PROJECT_ID",
  storageBucket: "PASTE_PROJECT_ID.firebasestorage.app",
  messagingSenderId: "PASTE_MESSAGING_SENDER_ID",
  appId: "PASTE_APP_ID"
};

// Shared Firestore document used by this onboarding workspace.
// You can change the ID later if you create additional onboarding plans.
export const workspaceId = "mary-fisher-ai-transformation";
